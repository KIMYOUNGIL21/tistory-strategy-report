# 인수인계 (HANDOFF)

> 이 문서는 **다른 PC / 새 Claude 세션에서 이 프로젝트를 이어받기 위한 맥락**이다.
> 원래는 Claude 로컬 메모리(`~/.claude/.../memory/tistory-*.md`)에만 있던 내용이라 저장소에 옮겨 두었다.
> 마지막 갱신: 2026-07-18

---

## 0. 새 PC에서 제일 먼저 할 일

1. 이 저장소 clone (`git clone https://github.com/KIMYOUNGIL21/tistory-strategy-report.git`)
2. **이 HANDOFF.md와 `posts/00-발행계획.md`를 먼저 읽는다.** 프로젝트의 모든 결정·상태가 그 두 곳에 있다.
3. 브라우저 자동화가 필요하면: Chrome + **Claude in Chrome 확장** 연결 + **티스토리 로그인**(ai-pd.tistory.com).
4. 이미지는 저장소 안 `assets/images/`에 있다(원래는 `~/ai/images/tistory/`에 있던 것).

---

## 1. 프로젝트 정체성

`티스토리 × AI 음악(Suno) 전자책 퍼널` (2026-07-16 시작).

- **퍼널**: 검색 유입 → 카카오 오픈채팅 → 그로블 무료판 → 39,000원 유료 전자책.
- **오픈채팅 링크**: https://open.kakao.com/o/glxOtmDi ("실시간 문의 + 무료 전자책")
- **블로그**: **ai-pd.tistory.com** — "AI PD (왕초보 입문 전문가)", 닉네임 AI.PD, 스킨 Odyssey(반응형).
- **채널 분담**: 티스토리 = **Suno 집중**(구글·다음 타깃, 네이버 헤드 키워드는 레드오션이라 포기). 네이버 블로그 = 노션의 AI 공부 정리 재가공. **두 채널에 같은 글 복사 금지**(다음 저품질 + 구글 중복 콘텐츠 리스크).
- **차별화**: 협찬 후기·강사형 시장의 반대편 — "실제 3채널 운영 장부 공개". 운영 채널: 그밤라디오/강한결의 트로트TV/중년가요방, 합산 구독자 1,164명(연내 1만 목표).
- **운영 리듬**: 90일간 블로그 1개, 주 3편(검색형 2 + 경험형 1). 월·수·금 아침 08:00 예약발행.

---

## 2. 발행 역할 분담 — **가장 중요한 안전 규칙**

**Claude는 비공개 저장까지만 한다. 예약·발행·공개 전환은 사용자가 직접 한다.**

**왜:** 티스토리 발행 창은 공개로 바꾸는 순간 발행일이 "현재"로 잡히고, 날짜를 바꾸기 전에 발행 버튼을 누르면 즉시 공개된다. 이 날짜 지정 버튼이 브라우저 자동화에 반응하지 않는다(좌표·요소·이벤트 강제 전부 실패, 사람이 누르면 정상). 자동화가 억지로 시도하면 즉시 발행 사고만 난다. 또 **비공개로 전환하면 기존 예약이 삭제된다.**

**적용:**
- 본문 수정은 편집기에서 하되, 저장은 "완료" → **비공개** → "비공개 저장"으로 끝낸다.
- 이미 예약이 걸린 글을 수정할 때는 공개/예약 설정을 **건드리지 말고 본문만 저장**하면 예약이 유지된다.
- 작업을 마치면 "어느 글을 언제로 예약" 목록을 정리해 사용자에게 넘긴다.

---

## 3. 글 와꾸(문체 규칙) — "글빨"이 이 프로젝트의 핵심

사용자는 이탈·재방문이 문체에 달렸다고 본다. 규칙:

- 훅 첫 문장(두괄식·수치) → 경험 근거 + 자기고백
- H3는 "N. 키워드 — 결론" 형태 / 문단 1~3문장
- 비교는 표 · 문제는 Q&A · 예시는 인용구(GEO 최적화)
- 이미지 캡션 "무엇 (연월. 직접 캡처)"
- 섹션당 구어체 1번
- 마무리는 다음 글 클리프행어 → **다섯 줄 요약(번호 리스트)** → **CTA 박스**(오픈채팅) → 작성일 꼬리
- 색깔·밑줄 금지, 볼드는 섹션당 0~1개
- 상세 규칙 12개는 `posts/01-suno-사용법-2026.md` 하단에 있다.

---

## 4. 절대 규칙: 경험담·수치 날조 금지

**1인칭 경험·수치는 사용자 확인 없이 절대 생성하지 않는다.** 와꾸의 "실수 고백" 칸을 채울 재료가 없으면 비워두고 `[입력]`으로 남긴다.

실제로 QA의 "실수 고백 없음" 지적을 채우려다 겪지 않은 일화 6건을 지어낸 적이 있다(2번 "저도 그 표를 믿었습니다", 4번 "무료 곡 전부 버렸다" 등) — 전부 삭제했다.

- 초안 마커: `[입력]`은 사용자 경험으로 채운다. `[확인]`은 발행 전 재확인한다.
- **날조 의심 문장은 지우기 전에 저장소·실물부터 대조할 것.** 8번 글에서 "날조" 의심 4건 중 3건이 실제로는 사실이었다(오탐).

---

## 5. QA 검수

발행 전 검수는 독립 에이전트 **`blog-qa`**(`~/.claude/agents/blog-qa.md`)로 한다. 10년차 블로거 관점에서 SEO·GEO·가독성·흐름을 냉정하게 본다.

- **검수는 마크다운이 아니라 티스토리 실제 글 기준으로 한다.** QA도 틀린다(실물 우선 원칙).
- 시리즈가 쌓일수록 **글 사이 사실 충돌**이 새 오류원이 된다(7번이 3번 글과 장르 설명이 충돌한 사례). 새 글은 앞선 글과 대조할 것.

---

## 6. 마크다운 ≠ 티스토리 (동기화 격차)

**로컬 마크다운은 티스토리 실제 글의 사본이 아니라 별개로 갈라진 버전이다.**

- 티스토리 실제 글에는 각 글마다 대표 이미지(타이틀 카드) + 스크린샷 + 내부 링크가 이미 들어가 있으나 마크다운엔 없는 경우가 많다.
- 크레딧 섹션 등 본문이 문장 단위로 다른 곳도 있다.
- **티스토리에서 직접 편집한 내용은 반드시 마크다운에 역반영할 것.**
- 남은 유물: 마크다운 몇 곳의 `([확인] 1번 글 URL)`은 실제 URL로 치환해야 한다(발행을 막진 않음).
- **각 글 대표 이미지가 마크다운에 기록 안 됨** — 아직 미해결 과제.

### 티스토리 편집 방법 (재현 가능, 검증됨)

- 편집기 URL: `https://ai-pd.tistory.com/manage/newpost/{entryId}?type=post&returnURL=%2Fmanage%2Fposts%2F`
- entryId는 글 관리 목록 체크박스 id(`inpCheck7` → 7)에서 얻는다.
- 본문은 TinyMCE — `window.tinymce.get('editor-tistory')`로 접근, `getDoc().body.children` 직접 수정 후 `fire('change')` + `setDirty(true)`.
- 저장: "완료" → 발행창에서 **설정 건드리지 말고** "공개 발행" → 발행일이 미래면 예약 유지.
- **이미지 삽입**: 파일 input이 DOM에 없어서 안 통함. **로컬 CORS 서버 + paste 이벤트 주입** 방식으로 넣는다.
- **UTM 부착 불가**: 티스토리가 저장 시 본문 링크의 쿼리스트링을 서버에서 제거한다(실측 2회). 유입 측정이 필요하면 오픈채팅 링크를 Cloudflare Worker 리다이렉트로 감싸는 방법 검토.

---

## 7. 캡처 함정 (실제로 당할 뻔함)

Suno 곡 페이지에서 **크롬 자동번역이 켜지면 한국어 가사를 또 한국어로 번역해 뭉갠다.** ("네가 내민 손을" → "내 네가 민손을"). UI도 Edit→"편집하다"로 바뀌니 그게 신호다. 새로고침하면 복구된다. **캡처 전 반드시 원문 상태인지 확인할 것.**

---

## 8. 인프라 주의

- **GSC 인증 메타태그가 Odyssey 스킨 HTML head 8행에 직접 삽입돼 있다 → 스킨을 바꾸면 사라진다.** 스킨 변경 시 "메타 태그 등록" 플러그인으로 옮길 것.
- 이미 완료: 포스트 문자(슬러그) 주소, RSS 전체공개 50개, 카테고리 4개, 모바일웹 자동연결 끄기, GSC 소유권 인증 + sitemap/rss 제출, 소개·개인정보처리방침 페이지 발행 + 메뉴 추가.

---

## 9. 현재 상태 요약 (2026-07-18 기준)

정확한 최신 상태는 항상 **`posts/00-발행계획.md`**를 신뢰할 것. 요약:

| # | 글 | 상태 |
|---|---|---|
| 1 | Suno 사용법 2026 | **발행 완료**(7/16). 4번 발행 후 저작권 글 내부 링크 추가 필요 |
| 2 | 유튜브 AI 음악 수익 정지 | 예약 7/18(토) 08:00 · 완료 |
| 3 | 3채널 1,164명까지의 과정 | 예약 7/20(월) 08:00 · 완료 |
| 4 | Suno 상업적 이용(저작권) | 예약 7/22(수) 08:00 · 완료 |
| 5 | Suno 요금제 비교 | 예약 7/24(금) 08:00 · 완료 |
| 6 | ~~AI 표시 체크리스트~~ | **폐기**(수요 없음+중복). git `4d8cb98`에 보존 |
| 7 | Suno 한글 발음 | 완성·QA 통과. 7/27(월) 예정. **차단: 5번 발행(7/24) 후 5번 URL 치환해야 발행 가능**. 티스토리 미반영 |
| 8 | 자동 리포트 | 본문 완성. 남은 것: 캡처 2장 + 월 비용 숫자 + 발행순서. 티스토리 미반영 |
| 9? | Suno 프롬프트 쓰는 법 | 신규 제안(SEO 최대 공백). 미착수 |

**사용자가 직접 할 다음 액션:**
1. 예약글은 두면 순서대로 자동 발행(7/18→7/20→7/22→7/24).
2. 4번 발행(7/22) 후: 1번 글에 4번 내부 링크 추가.
3. 5번 발행(7/24) 후: 7번 글의 5번 URL 치환 → 7번 예약 가능.

---

## 10. 저장소에 없는 것 (알아둘 것)

- `research/raw/` — deep research 원본. `.gitignore` 처리됨(재현 가능한 참고자료라 제외).
- Claude 로컬 메모리 원본은 이 PC의 `~/.claude/.../memory/tistory-*.md`에 있었다. 이 HANDOFF.md가 그 내용을 담고 있으므로 새 PC에선 이 문서를 신뢰하면 된다.
